extends layout

block content
  h1= article.title
  h5 Written by #{article.author.name}
  p.paragraph= article.body
  hr
  a.btn.btn-default(href='/articles/edit/'+article._id,style='margin-right: 10px') 修改
  a.btn.btn-danger.delete-article(href='#', data-id=article._id) 删除

